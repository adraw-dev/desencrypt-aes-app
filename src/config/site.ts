export type SiteConfig = typeof siteConfig;

export const siteConfig = {
  name: "adraw - AES256 Decryptor",
  description:
    "una app hecha para desemcriptar datos cifrados con AES256 para testear",
};
